spring:
  application:
    name: transfer

feign:
  account: http://localhost:8081
  exchange: http://localhost:8083

server:
  port: 8085

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
